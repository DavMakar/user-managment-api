apiVersion: v1
kind: Service
metadata:
  name: user-management-service
  namespace: user-management
spec:
  selector:
    app: user-management
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  type: LoadBalancer  # For external access